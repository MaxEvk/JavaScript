const nome = 'Max';
const sobreNome = 'Lima';
const idade = 30;
const peso = 84;
const altura = 1.68;
let imc; //peso / (altura * altura)
let anoNascimento;
anoNascimento = '1982';
imc = peso / (altura * altura);

console.log(nome, sobreNome, 'tem', idade, 'anos, pesa', peso, 'KG, tem', altura, 'de altura e seu IMC é de', imc, nome, sobreNome, 'nasceu em 1982');
